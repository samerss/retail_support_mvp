# Copy this file to `.env` (same folder) and fill in your key.
# ADK auto-loads .env from the agent package at startup.

# Google AI Studio API key for the Gemini model the agent uses.
# Create one at https://aistudio.google.com/apikey
GOOGLE_API_KEY=
